<Window x:Class="ARKServerCreationTool.ReliabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reliability" Height="560" Width="560" MinWidth="480" MinHeight="400">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Settings" Padding="8">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="Stagger between starts (seconds):" Width="220" VerticalAlignment="Center"/>
                    <TextBox x:Name="txt_stagger" Width="80"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="Backup folder:" Width="220" VerticalAlignment="Center"/>
                    <TextBox x:Name="txt_backupRoot" Width="220"/>
                    <Button x:Name="btn_browseBackup" Content="Browse" Width="70" Margin="6,0,0,0" Click="btn_browseBackup_Click"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="Keep last N backups:" Width="220" VerticalAlignment="Center"/>
                    <TextBox x:Name="txt_keep" Width="80"/>
                </StackPanel>
                <CheckBox x:Name="chk_crashRestart" Content="Auto-restart crashed servers" Margin="0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="Give up after N crashes:" Width="220" VerticalAlignment="Center"/>
                    <TextBox x:Name="txt_crashThreshold" Width="80"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <TextBlock Text="…within (minutes):" Width="220" VerticalAlignment="Center"/>
                    <TextBox x:Name="txt_crashWindow" Width="80"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Scheduled tasks" Padding="8" Margin="0,8,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ListBox x:Name="lst_tasks" Grid.Row="0" MinHeight="120"/>
                <WrapPanel Grid.Row="1" Margin="0,6,0,0">
                    <ComboBox x:Name="cmb_type" Width="90" Margin="0,0,6,4"/>
                    <ComboBox x:Name="cmb_target" Width="160" Margin="0,0,6,4"/>
                    <ComboBox x:Name="cmb_mode" Width="120" Margin="0,0,6,4" SelectionChanged="cmb_mode_Changed"/>
                    <TextBox x:Name="txt_when" Width="90" Margin="0,0,6,4" ToolTip="Daily: HH:mm — Interval: number of hours"/>
                    <Button x:Name="btn_addTask" Content="Add" Width="60" Margin="0,0,6,4" Click="btn_addTask_Click"/>
                    <Button x:Name="btn_removeTask" Content="Remove" Width="70" Margin="0,0,0,4" Click="btn_removeTask_Click"/>
                </WrapPanel>
            </Grid>
        </GroupBox>

        <Button Grid.Row="2" x:Name="btn_save" Content="Save" Width="90" Height="28" HorizontalAlignment="Right" Margin="0,8,0,0" Click="btn_save_Click"/>
    </Grid>
</Window>
